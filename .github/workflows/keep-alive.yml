name: Supabase Keep-Alive

on:
  schedule:
    - cron: "0 5 */3 * *"
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend keep-alive endpoint
        run: curl -fsS https://kosme-production.up.railway.app/api/keep-alive
